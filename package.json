{
  "name": "nod",
  "version": "0.0.0",
  "scripts": {
    "start-backend": "cd backend && npm run start",
    "start-frontend": "cd frontend && ng serve",
    "start": "npm run start-frontend & npm run start-backend",
    "postinstall": "ng build --output-path ./frontend/dist",
    "build": "ng build"
  },
  "dependencies": {
    "heroku": "^7.39.5"
  }
}
