import {Message} from "./Message";

class MessageDispatcher {

    dispatch(): Message {
        return null;
    }

   public execute(msg: Message) : Message {
        return null;
    }
}

export const messageDispatcher = new MessageDispatcher();